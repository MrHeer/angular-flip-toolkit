import { FlipAnimationDirective } from './flip-animation.directive';

describe('FlipAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
